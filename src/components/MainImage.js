import styles from "./MainImage.module.css";
const MainImage = ({ onClose }) => {
  return (
    <div className={styles.mainImage}>
      <img className={styles.image4Icon} alt="" src="/undefined21.png" />
    </div>
  );
};

export default MainImage;
